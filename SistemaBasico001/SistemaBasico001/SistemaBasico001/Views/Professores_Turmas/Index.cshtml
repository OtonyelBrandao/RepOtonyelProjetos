@model IEnumerable<SistemaBasico001.Models.Professores_Alunos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.professores.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.turmas.ano)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.professores.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.turmas.ano)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDProfessorTurma }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDProfessorTurma }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDProfessorTurma })
        </td>
    </tr>
}

</table>
