@model IEnumerable<SistemaBasico001.Models.Alunos_Professores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.alunos.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.professores.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.alunos.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.professores.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDFKAlunosProfessores }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDFKAlunosProfessores }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDFKAlunosProfessores })
        </td>
    </tr>
}

</table>
